# The control-plane label and NoSchedule taint are emitted by `talosctl gen config`; this
# only adds the LB exclusion (also a 1.14 default, kept explicit).
apiVersion: v1alpha1
kind: KubeNodeConfig
labels:
  node.kubernetes.io/exclude-from-external-load-balancers: ""
